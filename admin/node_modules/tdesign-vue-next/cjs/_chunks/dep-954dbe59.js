/**
 * tdesign v1.20.1
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

var _getTag = require('./dep-c51f4345.js');
var _baseUnary = require('./dep-2e9fed7b.js');
var isArray = require('./dep-d1d374b4.js');
var _overArg = require('./dep-a324ad6a.js');

/** `Object#toString` result references. */
var mapTag = '[object Map]',
  setTag = '[object Set]';

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Checks if `value` is an empty object, collection, map, or set.
 *
 * Objects are considered empty if they have no own enumerable string keyed
 * properties.
 *
 * Array-like values such as `arguments` objects, arrays, buffers, strings, or
 * jQuery-like collections are considered empty if they have a `length` of `0`.
 * Similarly, maps and sets are considered empty if they have a `size` of `0`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is empty, else `false`.
 * @example
 *
 * _.isEmpty(null);
 * // => true
 *
 * _.isEmpty(true);
 * // => true
 *
 * _.isEmpty(1);
 * // => true
 *
 * _.isEmpty([1, 2, 3]);
 * // => false
 *
 * _.isEmpty({ 'a': 1 });
 * // => false
 */
function isEmpty(value) {
  if (value == null) {
    return true;
  }
  if (_baseUnary.isArrayLike(value) && (isArray.isArray(value) || typeof value == 'string' || typeof value.splice == 'function' || _overArg.isBuffer(value) || _overArg.isTypedArray(value) || _baseUnary.isArguments(value))) {
    return !value.length;
  }
  var tag = _getTag.getTag(value);
  if (tag == mapTag || tag == setTag) {
    return !value.size;
  }
  if (_overArg.isPrototype(value)) {
    return !_getTag.baseKeys(value).length;
  }
  for (var key in value) {
    if (hasOwnProperty.call(value, key)) {
      return false;
    }
  }
  return true;
}

exports.isEmpty = isEmpty;
//# sourceMappingURL=dep-954dbe59.js.map
