spring:
  datasource:
    url: jdbc:mysql://localhost:3306/neoapp?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: liulei0304
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-timeout: 30000
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

# Tencent Cloud COS 对象存储
cos:
  secret-id: AKID2uXu0rTr7C2d082
  secret-key: UjiRu73jAPUJuGOG
  region: ap-shanghai
  bucket: neoimage-1251198067

# 微信配置
wechat:
  oauth:
    app-id:       # 公众号 AppID
    app-secret:   # 公众号 AppSecret
    redirect-uri: http://localhost:3001/login
  pay:
    app-id:            # 公众号/小程序 AppID
    mch-id:            # 商户号
    api-v3-key:        # API v3 密钥
    mch-serial-no:     # 商户证书序列号
    private-key-path:  # 商户私钥路径 (classpath资源路径)
    notify-url: http://localhost:8080/api/public/wechat-pay/callback

logging:
  level:
    com.neoapp: debug
    org.springframework.security: debug
